#include <stdio.h>
#include <stdlib.h>

int mmc (int n1, int n2)
{
int c = 0, r = 0;

	for (c = 1; c > 0; c++)
	{
		return r = (n1*c) % n2;
			if (r == 0) break;
	}	
	printf ("%d",n1*c);
	
    return 0;
		
	

}
int main ()
{

	int n1, n2;
	scanf ("%d %d", &n1, &n2);
	printf ("%d", mmc(n1, n2);
	return 0;
}
