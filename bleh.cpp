#include <stdio.h>

int main (){

    float pi = 0;
    int termos, i, sinal =1;

    scanf("%d", &termos);

    for (i =1; i<=termos; i++){

        pi = pi + (sinal(4/(float)(i2-1)));
        sinal = sinal *-1;

    }
    printf("%.5f",pi);
    return 0;
}
